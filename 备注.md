1.1.0
新增 配置文件（<TShock.SavePath>/LongNight/config.json）
新增 可配置关闭永夜（关闭后昼夜恢复自然长度，黎明事件照常触发）
新增 始终满月（不再轮换月相）
新增 黎明事件（游戏内 04:25~04:29 触发）
     - 更换月亮样式：按顺序轮流（cycle）/ 随机（random）/ 关闭（off）
     - 更换彩蛋特性：从池子里随机开启，支持终章恢复
     - 触发世界事件（天气 / 天象 / 派对）
         通用：meteor 陨石 / party 派对 / storm 暴风雨
         夜晚类（永夜开启时）：blood 血月 / lantern 灯笼夜 / starfall 流星雨
         白天类（永夜关闭时）：eclipse 日食 / slime 史莱姆雨 / sand 沙尘暴
         随机时按当前昼夜自动过滤，不会抽到无效事件
     - 月亮样式 + 彩蛋特性 + 世界事件 并成一行播报，例如「今天是 紫色的三重月亮, 吸血鬼, 血月」
新增 指令 /ln event、/ln moon、/ln style、/ln seed、/ln world、/ln restore、/ln reload
新增 权限节点 longnight
进度与「事件改动记录」持久化到 <TShock.SavePath>/LongNight/state.json
     - state.json 含 NightCurrent / TouchedSeeds / TouchedWorld，事件改动时立即落盘
     - 否则重启后 RestorePrevious 与终章恢复会找不到“上一次开了什么”，特性会一晚一晚叠加
新增 配置 RestoreOnStartup：重启后发现上次没恢复完，启动时自动补一次
     - 在第一个 GameUpdate 里做，确保世界（.wld）已加载
     - 该恢复不受插件总开关影响，避免脏状态永久留在世界里

1.0.0.3
永夜期间会更换月相和渔夫任务

1.0.0.1
永夜期间会更换渔夫任务

1.0.0.0
4:25~4:29 会判断是否进入下一个夜晚，这期间称为”永夜“
8天后退出永夜
如果是血月则维持血月状态 
月相设置到下一个月相

启动服务器    测试代码
```bash
wt "T:\TShock\@Server\S31\S31-forcetime\run.bat"
```
